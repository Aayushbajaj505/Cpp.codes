#include <bits/stdc++.h>
#include <algorithm>
#include <string>
#include <climits>
#include <vector>
#include <stack>
#include <queue>
using namespace std;
int main()
{
    int i, j, k = 0;
    int n1, n2;
    int arr1[n1];
    int arr2[n2];
    while (i < n1 && j < n2)
    {
        if (arr1[i] < arr2[j])
        {
            // arr3[k++]=arr[i++];
        }
        else
        {
        }
    }
}